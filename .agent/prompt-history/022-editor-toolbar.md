在**有选区状态**，在选区下方，增加一个工具条，提供手绘标注、矩形标注、直线标注三个功能，按下对应按钮，即选中工具，
此时鼠标在选区内的操作视为工具的操作，增加绘图的功能。在复制到剪贴板的时候，这些标注也要被复制进去。
工具栏的实现尽量独立，将代码放到单独的文件中，为每个工具都组织单独的文件，设计好可插拔的抽象。

---

工具栏的位置不对，应该显示在选区下面

---

还是不对，现在显示在了选区中上方，我需要显示在选区的下面，左边对齐，你考虑 dpi 了没有