增加通过 dbus 激活截图的功能
1. 【dbus 管理器】新增一个 dbus manager 的文件，用于统一管理 dbus 服务，
将之前的 window_lister 重构为使用这个 manager 管理，管理器应该具备统一的调度分发能力，其它组件把监听注册到管理器上
可以用 Qt 的信号实现
2. 【固定服务名】注册的 dbus 服务名字修改为固定的，将 pid 组件部分移除，方便其它软件调用
3. 【限制多实例】在启动的时候，检查 dbus 服务是否存在，如果存在，则通过 dbus 服务激活截图，而不是创建一个新实例
