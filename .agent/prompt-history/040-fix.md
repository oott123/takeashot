现在有这样的问题：鼠标点击截图区域外面，按下鼠标之后，截图区域会扩展，但是鼠标松开之后，自动变回去。预期应该是，截图区域自动扩展到鼠标点击位置并且不缩回去。写一个测试，暴露这个问题。

---

现在修复这个问题。

---

现在，需要在空白区域按下鼠标之后，和拖拽的处理一样，能拖动改变大小，就像是按下手柄拖拽到这个位置一样。注意如果点击的时候在单侧，拖动也不要能改变另一侧大小。比方说在左边点击就只能左右拖动，不能上下拖动，除非点击的地方需要移动两条边才能覆盖到。测试也跟着一起改掉。

---

逻辑不对，点击之后拖动，拖动到哪里，选区就到哪里。现在如果你在选区右边 100 的位置点击，选区会往右扩大 100 这没问题，
但是如果你继续往右移动50，选区会缩到 50 ，正确的应该是选区往右扩大 150（100+50）。

---

现在逻辑都对了。不过鼠标指针不对，现在鼠标指针放到选区外面，是十字架，改成八方向缩放的鼠标比较好。
在按下之前就可以改这个指针样式了。

---

四个角的指针反了。