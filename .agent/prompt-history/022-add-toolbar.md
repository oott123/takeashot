在**有选区状态**下，增加一个工具条。
1. 工具条的 UI 是一个长条型，宽度取决于按钮数量，高度 32，背景白色，边框黑色，按钮用图标表示，每个按钮都是方形，
按钮之间没有间隙，按钮本身也没有边框。
2. 工具条需要智能选择位置。优先在选区外、右对齐、下方。如果下方没有空间，那就选择上方。
如果选区外没有空间，才可以放到选区里，放在选区里面的右下方。
3. 在选区变化的时候，工具条需要跟着移动，并显示在屏幕上，不要被截图画面所遮挡。
4. 工具条实现要放到一个单独的文件里。
5. 工具条可以作为截图窗口的一个子 widget 实现。

---

在没有选区的屏幕上不要渲染工具栏。