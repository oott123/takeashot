用 pyqt 做一个 Linux 下的截图软件。截图的 demo 已经在 `screenshot_kwin.py` 里实现了，可以参考。启动的时候创建一个托盘图标。当点击托盘图标的时候，启动截图模式。截图模式下，先获得屏幕内容。然后按屏幕数量，将屏幕内容，渲染一个或多个窗口，覆盖全屏，并增加黑色半透明遮罩。用户可以用鼠标划选一个矩形区域。划选后，矩形内部将不再有遮罩，并且四周显示常见的 resize handle，用于选择截图区域。用户按回车键，此时将会对截图进行裁剪，将用户选中的区域，放到剪贴板上。用户按鼠标右键或者 esc，退出截图模式。​