重构工具栏功能，要求：
1. 加入 QML 渲染；将整个工具栏 widget 用 QML 渲染
2. 事件也用 QML ，解耦事件，QML 发信号，信号代表动作而非按钮名字，传递到 snipping window 处理
3. 重构之后更新 design.md ，其中本次没有修改的部分，不需要修改

---

Traceback (most recent call last):
  File "/home/alice/Repos/takeashot/snipping_widget.py", line 122, in mouseMoveEvent
    self.controller.on_mouse_move(global_pos)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^
  File "/home/alice/Repos/takeashot/main.py", line 380, in on_mouse_move
    self.update_snippets()
    ~~~~~~~~~~~~~~~~~~~~^^
  File "/home/alice/Repos/takeashot/main.py", line 412, in update_snippets
    snipper.update_toolbar_position()
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^
  File "/home/alice/Repos/takeashot/snipping_widget.py", line 238, in update_toolbar_position
    self.toolbar.resize(int(w), int(h))
                        ~~~^^^
TypeError: int() argument must be a string, a bytes-like object or a real number, not 'builtin_function_or_method'

---

工具栏有几个 UI 问题：
1. 鼠标悬浮上去会显示 tooltip ，类似 Close, Save, Copy 这些，这个悬浮的 tooltip 离按钮太近，把按钮挡住了，
可能是因为 qml 窗口太小，显示不下，可以把窗口高度弄高一点
2. qml 窗口高度不对，导致按钮底下有留白。可以把边框和背景色不要填充满窗口，让窗口透明，只有 toolbar 的区域填充
背景色和加边框就行了。