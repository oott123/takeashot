你需要阅读源码，并且更新 `docs/design.md` 设计文档。

内容需要包括：

1. 这个项目是干啥用的
2. 什么是拟选择（pending selection）状态，什么是有选区状态，什么是无选区状态
3. 这个项目的支持环境范围（KDE Wayland only）
4. 相关代码的大致位置（函数名、类名、文件名），不要包括具体行号

这份文档是为了帮助 agent 能更好地理解、定位代码，常识性内容不需要包含其中，每个小点用简洁的语言概括即可。
